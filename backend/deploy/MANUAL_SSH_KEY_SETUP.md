# 🔑 Ручная настройка SSH-ключа для Synology

## ⚠️ ВАЖНО: Вы добавили пароль вместо публичного ключа!

В строке 152 вы выполнили:
```bash
echo "6999LqJiQguX" >> ~/.ssh/authorized_keys
```

Это **пароль**, а не публичный ключ! Нужно добавить **публичный ключ**.

## ✅ Правильная последовательность

### Шаг 1: Получите публичный ключ на вашем компьютере

На вашем компьютере (PowerShell):

```powershell
Get-Content C:\Users\studo\.ssh\shortsai_synology.pub
```

Вы увидите что-то вроде:
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... synology-access
```

**Скопируйте всю эту строку!**

### Шаг 2: Подключитесь к Synology через VPS

```powershell
ssh root@159.255.37.158
# Введите пароль VPS

ssh admin@10.8.0.2
# Введите пароль Synology (6999LqJiQguX)
```

### Шаг 3: Очистите authorized_keys (удалите пароль)

На Synology выполните:

```bash
# Удалите неправильную запись с паролем
cat ~/.ssh/authorized_keys
# Найдите строку с "6999LqJiQguX" и удалите её вручную:
nano ~/.ssh/authorized_keys
# Или удалите файл и создайте заново:
rm ~/.ssh/authorized_keys
```

### Шаг 4: Добавьте правильный публичный ключ

На Synology выполните:

```bash
# Убедитесь, что директория существует
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавьте публичный ключ (вставьте строку из шага 1)
echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... synology-access" >> ~/.ssh/authorized_keys

# Установите правильные права
chmod 600 ~/.ssh/authorized_keys

# Проверьте содержимое
cat ~/.ssh/authorized_keys
```

**Важно:** В `authorized_keys` должна быть строка, начинающаяся с `ssh-ed25519` или `ssh-rsa`, а НЕ пароль!

### Шаг 5: Проверьте подключение

Выйдите из Synology и VPS, затем на вашем компьютере:

```powershell
# Прямое подключение (если доступно)
ssh -i C:\Users\studo\.ssh\shortsai_synology admin@192.168.100.222 'echo "OK"'

# Или через VPS
ssh root@159.255.37.158 "ssh -i /root/.ssh/shortsai_synology admin@10.8.0.2 'echo OK'"
```

Если подключение работает без пароля — готово! ✅

## Альтернативный способ: через один SSH-команду

Если у вас есть доступ к Synology, можно скопировать ключ одной командой:

### На вашем компьютере:

```powershell
# Прочитайте публичный ключ
$pubkey = Get-Content C:\Users\studo\.ssh\shortsai_synology.pub

# Скопируйте через VPS одной командой
ssh root@159.255.37.158 "ssh admin@10.8.0.2 'mkdir -p ~/.ssh && chmod 700 ~/.ssh && echo \"$pubkey\" >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys'"
```

## Проверка правильности настройки

После настройки проверьте на Synology:

```bash
# Проверьте содержимое authorized_keys
cat ~/.ssh/authorized_keys

# Должна быть строка вида:
# ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... synology-access
# НЕ должно быть строк с паролем или другими данными!
```

## Если что-то не работает

1. **Проверьте права доступа:**
   ```bash
   ls -la ~/.ssh/
   # Должно быть:
   # drwx------ .ssh
   # -rw------- authorized_keys
   ```

2. **Проверьте логи SSH на Synology:**
   ```bash
   tail -f /var/log/auth.log
   ```

3. **Проверьте, что SSH-ключ правильный:**
   ```powershell
   # На вашем компьютере
   ssh-keygen -l -f C:\Users\studo\.ssh\shortsai_synology.pub
   ```

## После успешной настройки

Обновите SSH config на вашем компьютере:

```powershell
# Добавьте в C:\Users\studo\.ssh\config:

Host synology-shortsai
    HostName 10.8.0.2
    User admin
    IdentityFile ~/.ssh/shortsai_synology
    IdentitiesOnly yes
    StrictHostKeyChecking accept-new
    ProxyJump root@159.255.37.158
```

Теперь можно подключаться просто:
```powershell
ssh synology-shortsai
```




