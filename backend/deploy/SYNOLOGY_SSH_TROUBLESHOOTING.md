# 🔧 Устранение проблем с SSH доступом к Synology

## Проблема: Permission denied

Если вы получаете ошибку `Permission denied (publickey,password)`, попробуйте следующие решения:

## ✅ Решение 1: Проверьте пользователя и пароль

**Возможные варианты:**
- Пользователь может быть не `admin`, а другой (например, ваш личный пользователь)
- Пароль может быть другим

**Попробуйте:**
```powershell
# Попробуйте с вашим пользователем Synology
scp backend\deploy\synology_deploy.sh ваш_пользователь@192.168.100.222:/tmp/
```

## ✅ Решение 2: Включите SSH на Synology

1. Войдите в DSM (веб-интерфейс Synology)
2. Control Panel → Terminal & SNMP
3. Убедитесь, что "Enable SSH service" включен
4. Проверьте порт SSH (по умолчанию 22)

## ✅ Решение 3: Используйте другой порт

Если SSH на другом порту:

```powershell
scp -P 2222 backend\deploy\synology_deploy.sh admin@192.168.100.222:/tmp/
```

## ✅ Решение 4: Подключитесь через VPS (VPN туннель)

Если Synology доступен через VPN на VPS:

```powershell
# 1. Скопируйте файл на VPS
scp backend\deploy\synology_deploy.sh root@159.255.37.158:/tmp/

# 2. Подключитесь к VPS
ssh root@159.255.37.158

# 3. На VPS скопируйте на Synology через VPN
scp /tmp/synology_deploy.sh admin@10.8.0.2:/tmp/
```

## ✅ Решение 5: Используйте QuickConnect или DDNS

Если у вас настроен QuickConnect:

```powershell
# Попробуйте через QuickConnect
scp backend\deploy\synology_deploy.sh admin@hotwell.synology.me:/tmp/
```

## ✅ Решение 6: Настройте SSH ключи

**С вашего компьютера:**

```powershell
# 1. Сгенерируйте SSH ключ (если нет)
ssh-keygen -t rsa -b 4096

# 2. Скопируйте ключ на Synology
ssh-copy-id admin@192.168.100.222
```

## ✅ Решение 7: Проверьте настройки пользователя в DSM

1. Войдите в DSM
2. Control Panel → User & Group
3. Выберите пользователя `admin`
4. Убедитесь, что:
   - Пользователь активен
   - SSH доступ разрешен
   - Пароль установлен

## ✅ Решение 8: Используйте веб-интерфейс для загрузки файлов

Если SSH не работает, используйте File Station в DSM:

1. Войдите в DSM
2. Откройте File Station
3. Загрузите файлы через веб-интерфейс в `/tmp/`
4. Подключитесь через веб-терминал DSM (если доступен)

## 🔍 Диагностика

**Проверьте доступность Synology:**

```powershell
# Проверка доступности
Test-NetConnection -ComputerName 192.168.100.222 -Port 22

# Или через ping
ping 192.168.100.222
```

**Проверьте SSH на Synology:**

```powershell
# Попробуйте подключиться с подробным выводом
ssh -v admin@192.168.100.222
```

---

**После решения проблемы SSH, выполните деплой по инструкции в DEPLOY_SYNOLOGY_NOW.md**





