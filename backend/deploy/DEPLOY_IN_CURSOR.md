# 🚀 Деплой через терминал Cursor с расширением Bash

## Запуск деплоя

Откройте **встроенный терминал Cursor** (Ctrl+`) и выполните:

```bash
cd C:\Users\studo\Downloads\p039-master\p039-master\backend
bash deploy/deploy_with_password.sh
```

Или для полного деплоя:

```bash
cd C:\Users\studo\Downloads\p039-master\p039-master\backend
bash deploy/full_synology_deploy.sh
```

## Что произойдёт

1. Скрипт попытается подключиться к Synology по SSH
2. **Будет запрошен пароль** для `admin@192.168.100.222`
3. Введите пароль: `6999LqJiQguX`
4. Скрипт скопирует код на Synology
5. Установит зависимости и запустит backend

## Если SSH-ключ не работает

Если скрипт не может подключиться без пароля, он запросит пароль интерактивно. Просто введите пароль когда попросит.

## Альтернатива: только копирование кода

Если нужно только обновить код:

```bash
cd C:\Users\studo\Downloads\p039-master\p039-master\backend
bash deploy/deploy_with_password.sh
```

Затем на Synology выполните:

```bash
ssh admin@192.168.100.222
cd /volume1/Hotwell/Backends/shortsai-backend
bash deploy/setup_on_synology.sh
```

## Настройка SSH-ключей (опционально)

Чтобы не вводить пароль каждый раз:

```bash
cd C:\Users\studo\Downloads\p039-master\p039-master\backend
bash deploy/setup_ssh_keys.sh
```

Это создаст SSH-ключ и скопирует его на Synology.

---

**Готово!** Запустите команду выше в терминале Cursor. 🎉




