# 📋 Отчёт о настройке SSH-ключей для Synology

## ✅ Что было сделано

### 1. Созданы скрипты настройки

- **`setup_ssh_keys.sh`** — Bash-скрипт для настройки SSH-ключей (для Git Bash/WSL)
- **`setup_ssh_keys.ps1`** — PowerShell-скрипт для настройки SSH-ключей (для Windows PowerShell)
- **`setup_sudo_nopasswd.sh`** — Опциональный скрипт для настройки sudo без пароля

### 2. Обновлены deploy-скрипты

- **`config.sh`** — Автоматически определяет путь к SSH-ключу `shortsai_synology`
  - Используется путь на Synology: `/volume1/shortsai/app/backend`
  - Добавлена переменная `SYNO_SSH_HOST` для использования SSH config host

- **`full_deploy.sh`** — Обновлён для использования SSH-ключа или SSH config host
  - Функция `run_synology()` использует `synology-shortsai` из SSH config
  - Функция `copy_to_synology()` использует SSH-ключ автоматически
  - Все команды теперь работают без запроса пароля

### 3. Создана документация

- **`SSH_KEYS_SETUP.md`** — Полная документация по настройке и использованию
- **`QUICK_SSH_SETUP.md`** — Быстрая инструкция для начала работы

## 📁 Изменённые файлы

### Новые файлы:
1. `backend/deploy/setup_ssh_keys.sh`
2. `backend/deploy/setup_ssh_keys.ps1`
3. `backend/deploy/setup_sudo_nopasswd.sh`
4. `backend/deploy/SSH_KEYS_SETUP.md`
5. `backend/deploy/QUICK_SSH_SETUP.md`
6. `backend/deploy/SSH_SETUP_REPORT.md` (этот файл)

### Обновлённые файлы:
1. `backend/deploy/config.sh`
   - Используется путь: `/volume1/shortsai/app/backend`
   - Добавлена автоматическая настройка SSH-ключа
   - Добавлена переменная `SYNO_SSH_HOST`

2. `backend/deploy/full_deploy.sh`
   - Обновлены функции `run_synology()` и `copy_to_synology()`
   - Добавлена поддержка SSH config host
   - Обновлены сообщения с использованием правильного SSH host

## 🚀 Как использовать

### Шаг 1: Настройка SSH-ключей

**Вариант A: Через Git Bash (рекомендуется)**
```bash
cd backend/deploy
bash setup_ssh_keys.sh
```

**Вариант B: Через PowerShell**
```powershell
cd backend\deploy
.\setup_ssh_keys.ps1
```

**Вариант C: Вручную**
См. инструкцию в `QUICK_SSH_SETUP.md`

### Шаг 2: Проверка подключения

```bash
ssh synology-shortsai 'echo "OK"; pwd'
```

Если подключение работает без пароля — готово! ✅

### Шаг 3: Деплой без пароля

```bash
cd backend/deploy
bash full_deploy.sh
```

Теперь деплой не будет запрашивать пароль! 🎉

### Шаг 4: Опционально — Настройка sudo без пароля

Если нужно, чтобы команды `sudo npm`, `sudo pm2` не запрашивали пароль:

```bash
cd backend/deploy
bash setup_sudo_nopasswd.sh
```

⚠️ **ВНИМАНИЕ**: Разрешает sudo без пароля только для конкретных команд (npm, node, pm2).

## 📝 Полезные команды

После настройки SSH-ключей вы можете использовать:

```bash
# Подключение к Synology
ssh synology-shortsai

# Просмотр логов
ssh synology-shortsai 'pm2 logs shortsai-backend'

# Статус pm2
ssh synology-shortsai 'pm2 status'

# Перезапуск backend
ssh synology-shortsai 'cd /volume1/shortsai/app/backend && pm2 restart shortsai-backend'

# Редактирование .env
ssh synology-shortsai 'nano /volume1/shortsai/app/backend/.env'

# Просмотр файлов
ssh synology-shortsai 'ls -la /volume1/shortsai/app/backend'
```

## 🔐 Безопасность

- ✅ SSH-ключ создан без парольной фразы для автоматизации
- ✅ Используется отдельный ключ `shortsai_synology` (не основной `id_rsa`)
- ✅ Ключ автоматически используется в deploy-скриптах
- ✅ Пароль больше не требуется для деплоя

**Важно:**
- ❌ **НЕ** коммитьте приватный ключ в Git
- ❌ **НЕ** показывайте содержимое приватного ключа
- ✅ Регулярно проверяйте `~/.ssh/authorized_keys` на Synology

## 📍 Расположение файлов

### На локальной машине:
- SSH-ключ: `~/.ssh/shortsai_synology` (или `C:\Users\ВашеИмя\.ssh\shortsai_synology` на Windows)
- SSH config: `~/.ssh/config` (или `C:\Users\ВашеИмя\.ssh\config` на Windows)
- Публичный ключ: `~/.ssh/shortsai_synology.pub`

### На Synology:
- Публичный ключ добавлен в: `~/.ssh/authorized_keys`
- Backend путь: `/volume1/shortsai/app/backend`

## ⚠️ Важные изменения

### Путь на Synology:
- **Используется:** `/volume1/shortsai/app/backend`

Все скрипты автоматически используют этот путь через переменные в `config.sh`.

## 🎯 Следующие шаги

1. ✅ Запустите `setup_ssh_keys.sh` или `setup_ssh_keys.ps1`
2. ✅ Проверьте подключение: `ssh synology-shortsai`
3. ✅ Запустите деплой: `cd backend/deploy && bash full_deploy.sh`
4. ✅ (Опционально) Настройте sudo: `bash setup_sudo_nopasswd.sh`

## 📚 Дополнительная документация

- Полная документация: `backend/deploy/SSH_KEYS_SETUP.md`
- Быстрый старт: `backend/deploy/QUICK_SSH_SETUP.md`
- Общая документация деплоя: `backend/deploy/DEPLOY_README.md`

---

**Готово!** Теперь вы можете деплоить без ввода пароля одной командой! 🚀

