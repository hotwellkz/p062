# 🔧 Исправление проблемы с окончаниями строк (CRLF → LF)

## Проблема

При копировании файлов из Windows на Linux возникает ошибка:
```
/root/synology-port-forward.sh: line 2: $'\r': command not found
```

Это происходит потому, что Windows использует CRLF (`\r\n`), а Linux - LF (`\n`).

## ✅ Быстрое исправление на VPS

Выполните на VPS:

```bash
# Конвертируем файл из CRLF в LF
sed -i 's/\r$//' /root/synology-port-forward.sh

# Проверяем, что файл теперь правильный
file /root/synology-port-forward.sh

# Запускаем скрипт
bash /root/synology-port-forward.sh
```

## 🔧 Альтернативные способы

### Вариант 1: Использовать dos2unix (если установлен)

```bash
# Установить dos2unix (если нет)
apt-get update && apt-get install -y dos2unix

# Конвертировать файл
dos2unix /root/synology-port-forward.sh

# Запустить
bash /root/synology-port-forward.sh
```

### Вариант 2: Использовать tr

```bash
tr -d '\r' < /root/synology-port-forward.sh > /root/synology-port-forward-fixed.sh
chmod +x /root/synology-port-forward-fixed.sh
bash /root/synology-port-forward-fixed.sh
```

### Вариант 3: Исправить при копировании

При копировании файла используйте `scp` с опцией, которая сохраняет правильные окончания, или конвертируйте сразу:

```powershell
# На Windows перед копированием можно использовать Git Bash:
# В Git Bash:
cd /c/Users/studo/Downloads/p039-master/p039-master
dos2unix backend/vps/synology-port-forward.sh
scp backend/vps/synology-port-forward.sh root@159.255.37.158:/root/
```

## 📝 Что делать прямо сейчас

**На VPS выполните:**

```bash
# 1. Конвертируем файл
sed -i 's/\r$//' /root/synology-port-forward.sh

# 2. Проверяем права
chmod +x /root/synology-port-forward.sh

# 3. Запускаем
bash /root/synology-port-forward.sh
```

## 🛡️ Предотвращение проблемы в будущем

Убедитесь, что в репозитории есть `.gitattributes` с правильными настройками:

```
*.sh text eol=lf
```

Это гарантирует, что все `.sh` файлы будут иметь LF окончания строк.

---

**Готово! После исправления скрипт должен запуститься! 🚀**





