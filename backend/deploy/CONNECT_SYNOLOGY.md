# 🔌 Подключение к Synology через VPS

## Проблема

Synology недоступен напрямую с вашего компьютера (timeout), но доступен через VPN туннель с VPS.

## ✅ Правильный способ подключения

### Вариант 1: Через VPS (рекомендуется)

```bash
# 1. Подключитесь к VPS
ssh root@185.104.248.130

# 2. С VPS подключитесь к Synology через VPN
ssh admin@10.8.0.2
```

### Вариант 2: Одной командой (SSH туннель)

```bash
# С вашего компьютера
ssh -J root@185.104.248.130 admin@10.8.0.2
```

### Вариант 3: Настройка SSH config

Создайте файл `~/.ssh/config`:

```
Host vps
    HostName 185.104.248.130
    User root

Host synology
    HostName 10.8.0.2
    User admin
    ProxyJump vps
```

Теперь можно подключаться просто:
```bash
ssh synology
```

## 🔍 Проверка доступности

### С VPS проверьте VPN туннель:

```bash
# На VPS
ping 10.8.0.2
ssh admin@10.8.0.2 "echo 'Synology доступен!'"
```

### Проверка SSH на Synology:

```bash
# На VPS
ssh -v admin@10.8.0.2
```

## 📝 Быстрые команды

```bash
# Подключение к Synology через VPS
ssh root@185.104.248.130 "ssh admin@10.8.0.2"

# Выполнение команды на Synology
ssh root@185.104.248.130 "ssh admin@10.8.0.2 'pm2 status'"

# Копирование файла на Synology через VPS
scp file.txt root@185.104.248.130:/tmp/
ssh root@185.104.248.130 "scp /tmp/file.txt admin@10.8.0.2:/volume1/shortsai/"
```

## ⚙️ Настройка автодеплоя

Скрипты автодеплоя (`full_deploy.sh`) автоматически подключаются к Synology через VPS, используя VPN IP `10.8.0.2`.

Убедитесь, что в `config.sh` установлено:
```bash
export SYNO_HOST="10.8.0.2"  # VPN IP Synology
```

---

**Готово! Теперь вы можете подключаться к Synology через VPS! 🚀**





