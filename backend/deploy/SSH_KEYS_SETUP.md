# 🔐 Настройка SSH-ключей для Synology

Этот документ описывает настройку SSH-ключей для автоматического доступа к Synology NAS без ввода пароля.

## Быстрый старт

### 1. Запустите скрипт настройки

```bash
cd backend/deploy
bash setup_ssh_keys.sh
```

Скрипт автоматически:
- Создаст SSH-ключ `shortsai_synology` в `~/.ssh/`
- Скопирует публичный ключ на Synology (потребуется ввести пароль один раз)
- Настроит `~/.ssh/config` для удобного доступа
- Проверит подключение

### 2. Проверьте подключение

```bash
ssh synology-shortsai 'echo "OK"; pwd'
```

Если подключение работает без пароля — готово! ✅

## Что было сделано

### Созданные файлы

1. **`~/.ssh/shortsai_synology`** — приватный SSH-ключ (НЕ показывайте его содержимое!)
2. **`~/.ssh/shortsai_synology.pub`** — публичный SSH-ключ (скопирован на Synology)
3. **`~/.ssh/config`** — конфигурация SSH с хостом `synology-shortsai`

### Обновлённые файлы

1. **`backend/deploy/config.sh`** — автоматически определяет путь к ключу
2. **`backend/deploy/full_deploy.sh`** — использует SSH-ключ или SSH config host
3. **`backend/deploy/setup_ssh_keys.sh`** — скрипт настройки ключей
4. **`backend/deploy/setup_sudo_nopasswd.sh`** — опциональный скрипт для sudo без пароля

### Пути на Synology

Используется путь:
- **`/volume1/shortsai/app/backend`** (правильный путь, указанный в config.sh)

## Использование

### Подключение к Synology

```bash
# Через SSH config (рекомендуется)
ssh synology-shortsai

# Или напрямую с ключом
ssh -i ~/.ssh/shortsai_synology admin@192.168.100.222
```

### Деплой без пароля

```bash
cd backend/deploy
bash full_deploy.sh
```

Теперь деплой не будет запрашивать пароль для Synology! 🚀

### Полезные команды

```bash
# Просмотр логов
ssh synology-shortsai 'pm2 logs shortsai-backend'

# Статус pm2
ssh synology-shortsai 'pm2 status'

# Перезапуск backend
ssh synology-shortsai 'cd /volume1/shortsai/app/backend && pm2 restart shortsai-backend'

# Редактирование .env
ssh synology-shortsai 'nano /volume1/shortsai/app/backend/.env'
```

## Опционально: Настройка sudo без пароля

Если нужно, чтобы команды `sudo npm`, `sudo pm2` не запрашивали пароль:

```bash
cd backend/deploy
bash setup_sudo_nopasswd.sh
```

⚠️ **ВНИМАНИЕ**: Скрипт разрешает sudo без пароля только для конкретных команд (npm, node, pm2), не для всех команд.

## Устранение проблем

### Ключ не работает

1. Проверьте, что публичный ключ скопирован на Synology:
   ```bash
   ssh synology-shortsai 'cat ~/.ssh/authorized_keys | grep shortsai'
   ```

2. Проверьте права доступа на Synology:
   ```bash
   ssh synology-shortsai 'ls -la ~/.ssh/'
   ```
   Должны быть:
   - `~/.ssh` — права 700
   - `~/.ssh/authorized_keys` — права 600

3. Проверьте SSH config:
   ```bash
   cat ~/.ssh/config | grep -A 5 synology-shortsai
   ```

### Подключение через VPS

Если Synology недоступен напрямую, но доступен через VPS:

1. Настройте ключ для VPS аналогично
2. Скрипты автоматически используют VPN туннель через VPS

### Windows Git Bash

Если используете Git Bash на Windows:
- Путь к ключу: `C:\Users\ВашеИмя\.ssh\shortsai_synology`
- SSH config: `C:\Users\ВашеИмя\.ssh\config`

## Безопасность

- ✅ **НЕ** коммитьте приватный ключ в Git
- ✅ **НЕ** показывайте содержимое приватного ключа
- ✅ Используйте отдельный ключ для Synology (не основной `id_rsa`)
- ✅ Регулярно проверяйте `~/.ssh/authorized_keys` на Synology

## Дополнительная информация

- SSH-ключ создан без парольной фразы для автоматизации
- Если нужна большая безопасность, можно добавить парольную фразу, но тогда потребуется `ssh-agent`
- Все deploy-скрипты теперь используют ключ автоматически

