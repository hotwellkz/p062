# 🚀 Быстрый старт деплоя

## ⚠️ ВАЖНО: Откуда запускать

### ✅ ПРАВИЛЬНО: С локального компьютера

```bash
# На вашем Windows компьютере (Git Bash)
cd C:\Users\studo\Downloads\p039-master\p039-master\backend
bash deploy/full_synology_deploy.sh
```

### ❌ НЕПРАВИЛЬНО: На Synology

```bash
# НЕ ДЕЛАЙТЕ ТАК:
ssh admin@192.168.100.222
bash deploy/full_synology_deploy.sh  # ❌ Это не сработает!
```

## Если вы уже на Synology

Если вы уже подключены к Synology и хотите только установить зависимости и запустить:

```bash
# На Synology
cd /volume1/Hotwell/Backends/shortsai-backend
# Или если путь другой:
cd /volume1/Backends/shortsai-backend
# Или если вы в deploy:
cd ..

# Затем запустите:
bash deploy/setup_on_synology.sh
```

Этот скрипт:
- ✅ Установит зависимости
- ✅ Соберёт проект
- ✅ Запустит через PM2

Но **НЕ** скопирует код с локального компьютера!

## Полный деплой (с копированием кода)

Для полного деплоя с копированием кода **всегда запускайте с локального компьютера**:

```bash
# На вашем компьютере
cd C:\Users\studo\Downloads\p039-master\p039-master\backend
bash deploy/full_synology_deploy.sh
```

## Устранение проблем

### "Permission denied" при SSH

Если SSH требует пароль, введите его когда запросит. Или настройте SSH-ключи:
```bash
# На локальном компьютере
bash deploy/setup_ssh_keys.sh
```

### "No such file or directory"

Убедитесь, что вы запускаете скрипт из правильной директории:
```bash
# Проверьте текущую директорию
pwd
# Должно быть: .../p039-master/backend
```

### "bash: command not found"

Установите Git for Windows (включает Git Bash):
- https://git-scm.com/download/win
