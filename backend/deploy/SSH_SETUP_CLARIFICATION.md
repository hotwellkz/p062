# ⚠️ Важное уточнение по настройке SSH-ключей

## ❌ Ошибка: Где запускать скрипты

**Скрипты настройки SSH-ключей (`setup_ssh_keys.sh` и `setup_ssh_keys.ps1`) нужно запускать на ЛОКАЛЬНОЙ машине (вашем компьютере), а НЕ на Synology!**

## ✅ Правильная последовательность действий

### Шаг 1: На ЛОКАЛЬНОЙ машине (ваш компьютер)

Откройте **Git Bash** или **PowerShell** на вашем компьютере и выполните:

```bash
# Перейдите в папку проекта на ЛОКАЛЬНОЙ машине
cd C:\Users\studo\Downloads\p039-master\p039-master\backend\deploy

# Запустите скрипт настройки (на ЛОКАЛЬНОЙ машине!)
bash setup_ssh_keys.sh
```

Или через PowerShell:
```powershell
cd C:\Users\studo\Downloads\p039-master\p039-master\backend\deploy
.\setup_ssh_keys.ps1
```

### Шаг 2: Что делает скрипт

Скрипт на ЛОКАЛЬНОЙ машине:
1. Создаёт SSH-ключ на вашем компьютере: `~/.ssh/shortsai_synology`
2. Копирует публичный ключ на Synology (потребуется ввести пароль один раз)
3. Настраивает `~/.ssh/config` на вашем компьютере
4. Проверяет подключение

### Шаг 3: Проверка на ЛОКАЛЬНОЙ машине

После настройки на вашем компьютере проверьте:

```bash
# На ЛОКАЛЬНОЙ машине
ssh synology-shortsai 'echo "OK"; pwd'
```

Если подключение работает без пароля — готово! ✅

## 📍 Текущая ситуация на Synology

На Synology реальный путь к backend:
- ✅ `/volume1/shortsai/app/backend` — **это правильный путь**
- ❌ `/volume1/Hotwell/Backends/shortsai-backend` — **этого пути не существует**

Путь в `config.sh` исправлен обратно на `/volume1/shortsai/app/backend`.

## 🔍 Что проверить на Synology

Если вы уже подключены к Synology через SSH, проверьте:

```bash
# На Synology
cd /volume1/shortsai/app/backend
pwd
ls -la

# Проверьте, что backend существует
ls -la dist/
ls -la package.json
```

## 🚀 После настройки SSH-ключей

После того, как вы настроите SSH-ключи на ЛОКАЛЬНОЙ машине, деплой будет работать так:

```bash
# На ЛОКАЛЬНОЙ машине
cd C:\Users\studo\Downloads\p039-master\p039-master\backend\deploy
bash full_deploy.sh
```

Скрипт автоматически:
1. Подключится к Synology по SSH-ключу (без пароля)
2. Скопирует файлы на Synology
3. Запустит деплой на Synology
4. Проверит результат

## 📝 Резюме

- ✅ **ЛОКАЛЬНАЯ машина**: Запускайте `setup_ssh_keys.sh` здесь
- ✅ **Synology**: Только проверяйте результат, скрипты настройки здесь не нужны
- ✅ **Путь на Synology**: `/volume1/shortsai/app/backend` (исправлено в config.sh)




