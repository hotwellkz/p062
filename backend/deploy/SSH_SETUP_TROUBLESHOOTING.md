# 🔧 Устранение проблем с настройкой SSH-ключей

## Проблема: Permission denied при подключении к Synology

Если вы получаете ошибку "Permission denied" при попытке скопировать SSH-ключ на Synology, попробуйте следующие решения:

### Решение 1: Проверьте пароль

Убедитесь, что вы вводите правильный пароль для пользователя `admin` на Synology.

### Решение 2: Используйте подключение через VPS

Если прямое подключение к Synology не работает, используйте скрипт через VPS:

```powershell
cd C:\Users\studo\Downloads\p039-master\p039-master\backend\deploy
.\setup_ssh_keys_via_vps.ps1
```

Этот скрипт:
1. Создаст SSH-ключ на вашем компьютере
2. Скопирует ключ на VPS
3. С VPS скопирует ключ на Synology через VPN туннель (10.8.0.2)

### Решение 3: Ручное копирование ключа

Если автоматическое копирование не работает, скопируйте ключ вручную:

#### Шаг 1: Просмотрите публичный ключ

```powershell
Get-Content C:\Users\studo\.ssh\shortsai_synology.pub
```

#### Шаг 2: Подключитесь к Synology (через VPS или напрямую)

```bash
# Через VPS
ssh root@159.255.37.158
ssh admin@10.8.0.2

# Или напрямую (если доступен)
ssh admin@192.168.100.222
```

#### Шаг 3: На Synology выполните:

```bash
# Создайте директорию .ssh если её нет
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавьте публичный ключ (скопируйте содержимое из шага 1)
echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ_ЗДЕСЬ" >> ~/.ssh/authorized_keys

# Установите правильные права
chmod 600 ~/.ssh/authorized_keys
```

#### Шаг 4: Проверьте подключение

На вашем компьютере:

```powershell
ssh -i C:\Users\studo\.ssh\shortsai_synology admin@192.168.100.222 'echo "OK"'
```

Или через VPS:

```powershell
ssh root@159.255.37.158 "ssh -i /root/.ssh/shortsai_synology admin@10.8.0.2 'echo OK'"
```

### Решение 4: Проверьте настройки SSH на Synology

Убедитесь, что на Synology:
1. SSH включён (Control Panel → Terminal & SNMP → Enable SSH service)
2. Порт SSH правильный (обычно 22)
3. Пользователь `admin` имеет права на SSH

### Решение 5: Используйте другой способ аутентификации

Если пароль не работает, возможно:
1. Пароль изменился
2. Учётная запись `admin` заблокирована
3. Требуется двухфакторная аутентификация

Проверьте настройки в DSM (веб-интерфейс Synology).

## Проверка подключения

### Проверка прямого подключения:

```powershell
# Тест подключения
ssh -v admin@192.168.100.222 'echo "Test"'
```

### Проверка через VPS:

```powershell
# Подключение к VPS
ssh root@159.255.37.158

# С VPS подключение к Synology
ssh admin@10.8.0.2 'echo "Test"'
```

## Альтернативный способ: Используйте существующее подключение

Если у вас уже есть рабочий SSH доступ к Synology (например, через другой ключ или пароль), вы можете:

1. Подключиться к Synology любым способом
2. Вручную добавить публичный ключ в `~/.ssh/authorized_keys`
3. Проверить подключение с новым ключом

## После успешной настройки

После того, как SSH-ключ настроен, обновите SSH config:

```powershell
# Добавьте в C:\Users\studo\.ssh\config:

Host synology-shortsai
    HostName 192.168.100.222
    User admin
    IdentityFile ~/.ssh/shortsai_synology
    IdentitiesOnly yes
    StrictHostKeyChecking accept-new
```

Или если используете VPS:

```
Host synology-shortsai
    HostName 10.8.0.2
    User admin
    IdentityFile ~/.ssh/shortsai_synology
    IdentitiesOnly yes
    StrictHostKeyChecking accept-new
    ProxyJump root@159.255.37.158
```

## Дополнительная помощь

Если ничего не помогает:
1. Проверьте логи SSH на Synology: `/var/log/auth.log`
2. Проверьте настройки файрвола на Synology
3. Убедитесь, что вы в той же сети, что и Synology (для прямого подключения)




