# 🪟 Запуск деплоя из Windows (PowerShell)

## Проблема

В PowerShell команды `chmod` и прямой запуск `.sh` скриптов не работают, так как это Unix-команды.

## ✅ Решение

### Вариант 1: Использовать Git Bash (рекомендуется)

Если у вас установлен Git for Windows, используйте Git Bash:

1. Откройте Git Bash
2. Перейдите в директорию проекта:
   ```bash
   cd /c/Users/studo/Downloads/p039-master/p039-master/backend/deploy
   ```
3. Запустите скрипт:
   ```bash
   chmod +x *.sh
   ./full_deploy.sh
   ```

### Вариант 2: Использовать WSL (Windows Subsystem for Linux)

Если у вас установлен WSL:

1. Откройте WSL (Ubuntu)
2. Перейдите в директорию проекта:
   ```bash
   cd /mnt/c/Users/studo/Downloads/p039-master/p039-master/backend/deploy
   ```
3. Запустите скрипт:
   ```bash
   chmod +x *.sh
   ./full_deploy.sh
   ```

### Вариант 3: Использовать bash в PowerShell (если установлен)

Если у вас установлен Git Bash:

```powershell
# В PowerShell
cd backend\deploy
bash full_deploy.sh
```

**Или используйте готовые скрипты:**

```powershell
# PowerShell скрипт
cd backend\deploy
.\deploy.ps1
```

```powershell
# Batch файл (в PowerShell используйте .\ перед именем)
cd backend\deploy
.\START_DEPLOY.bat
```

```cmd
# Batch файл (в CMD или двойной клик)
cd backend\deploy
START_DEPLOY.bat
```

### Вариант 4: Запустить через SSH на Linux-машине

Скопируйте скрипты на Linux-машину (VPS или другой сервер) и запустите там:

```powershell
# С вашего компьютера (PowerShell)
scp -r backend\deploy root@159.255.37.158:/root/shortsai-deploy
ssh root@159.255.37.158
cd /root/shortsai-deploy
chmod +x *.sh
./full_deploy.sh
```

## 🔧 Альтернатива: Ручной деплой

Если скрипты не работают, можно выполнить деплой вручную:

### 1. Настройка VPS

```powershell
# ВАЖНО: Выполняйте из корня проекта (p039-master), не из backend/deploy!

# Вернитесь в корень проекта
cd C:\Users\studo\Downloads\p039-master\p039-master

# Скопируйте скрипт на VPS
scp backend\vps\synology-port-forward.sh root@159.255.37.158:/root/

# Подключитесь к VPS
ssh root@159.255.37.158

# На VPS выполните:
chmod +x /root/synology-port-forward.sh
bash /root/synology-port-forward.sh
```

### 2. Деплой на Synology

```powershell
# ВАЖНО: Выполняйте из корня проекта (p039-master), не из backend/deploy!

# Вернитесь в корень проекта
cd C:\Users\studo\Downloads\p039-master\p039-master

# Скопируйте скрипт на Synology
scp backend\deploy\synology_deploy.sh admin@192.168.100.222:/tmp/

# Подключитесь к Synology
ssh admin@192.168.100.222

# На Synology выполните:
chmod +x /tmp/synology_deploy.sh
bash /tmp/synology_deploy.sh
```

## 📝 Рекомендация

**Лучший вариант для Windows:** 

1. **Установите Git for Windows** (включает Git Bash) - самый простой способ
   - Скачайте: https://git-scm.com/download/win
   - После установки используйте Git Bash для запуска скриптов

2. **Или используйте готовые обёртки:**
   - `deploy.ps1` - PowerShell скрипт
   - `START_DEPLOY.bat` - Batch файл (можно запустить двойным кликом)

3. **Или используйте WSL** для полноценной Unix-среды

---

**Готово! Выберите подходящий вариант для вашей системы! 🚀**

